# Defaults for kafka initscript
# sourced by /etc/init.d/kafka
# installed at /etc/default/kafka by the maintainer scripts

#
# This is a POSIX shell fragment
#

# Additional options that are passed to the Daemon.
DAEMON_OPTS=""
